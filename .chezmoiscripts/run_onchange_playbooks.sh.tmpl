#!/bin/bash

# .playbooks/user-env.yml hash: {{ include ".playbooks/user-env.yml" | sha256sum }}

if command -v ansible-playbook &> /dev/null; then
    ansible-playbook {{ joinPath .chezmoi.sourceDir ".playbooks/user-env.yml" | quote }} --ask-become-pass
fi
