#!/bin/bash

# .playbooks/user-env.yml hash: {{ include "dot_playbooks/user-env.yml" | sha256sum }}

if command -v ansible-playbook &> /dev/null; then
    ansible-playbook {{ joinPath .chezmoi.sourceDir "dot_playbooks/user-env.yml" | quote }} --ask-become-pass
fi
